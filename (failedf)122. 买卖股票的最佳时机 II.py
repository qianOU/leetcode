class Solution:
    def maxProfit(self, prices) -> int:
        n = len(prices)
        ans = 0
        flag = 1
        prev = 100000
        for i in range(n):
            if not flag and prev :


print(Solution().maxProfit([1, 2, 3, 4, 5]))